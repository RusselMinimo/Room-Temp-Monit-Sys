# Database Configuration
# Neon Postgres connection string (required for production)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Public App URL (used for metadata and absolute URLs)
NEXT_PUBLIC_APP_URL=https://your-domain.com

# Authentication
# Default admin credentials (automatically created on first access)
AUTH_EMAIL=admin@iot.local
AUTH_PASSWORD=russelpogi
# Optional: Pre-hashed password (if provided, AUTH_PASSWORD is ignored)
# AUTH_PASSWORD_HASH=

# Localhost Testing:
# For quick testing, you can:
# 1. Use the default admin credentials above
# 2. Or run: npm run db:seed (creates test users)
# See LOCALHOST_TESTING.md for more details

# Session configuration
AUTH_SESSION_TTL_SECONDS=28800  # 8 hours

# IoT API Configuration
# API key for device data ingestion (optional, if not set, no authentication required)
IOT_API_KEY=your-secret-api-key-here

# Google OAuth (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback

# Environment
NODE_ENV=development
